name: ci 

on:
  pull_request:
    branches: [main]

jobs:
  job-check:
    runs-on: ubuntu-latest
    steps:
    - name: check 
      env:
        GITHUB_CONTEXT: ${{ github.event.action }}
      run: echo "$GITHUB_CONTEXT"

    